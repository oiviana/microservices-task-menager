# web

## Propósito

Frontend em Vite + React. Status: ainda não iniciado — placeholder para documentação futura.

## Porta (desenvolvimento)

- 3000 (conforme `docker-compose.yml`)

## Como iniciar (quando pronto)

```bash
cd apps/web
yarn install
yarn dev
```

## Notas

- Dockerfile e mapeamentos já existentes em `docker-compose.yml` para desenvolvimento.
- Quando o frontend estiver implementado, adicionar rotas, dependências e instruções de build/produção.
